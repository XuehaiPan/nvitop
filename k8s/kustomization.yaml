apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: nvitop-exporter
  namespace: monitoring

resources:
- namespace.yaml
- serviceaccount.yaml
- daemonset.yaml
- service.yaml
- servicemonitor.yaml

commonLabels:
  app.kubernetes.io/name: nvitop-exporter
  app.kubernetes.io/component: gpu-monitoring
  app.kubernetes.io/part-of: monitoring

images:
- name: ghcr.io/ntheanh201/nvitop
  newTag: exporter-v1.5.4
